extends base.pug 

block content 
    if errorMessage
        span=errorMessage 
    form(method="POST")
        input(placeholder="Title", required, type="text", name="Title")
        input(placeholder="Description", required, type="text", name="description")
        input(type="Hastags, separate by comma.",required, name="hashtags")
        input(placeholder="submit", required, value="Upload a video")

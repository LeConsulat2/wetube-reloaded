doctype html
html(lang="en")
    head
        title #{pageTitle} | WeTube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        header
            h1=pageTitle
            nav 
                ul 
                    li 
                        a(hre="/videos/upload") Upload Video
        main 
            block content
    include partials/footer

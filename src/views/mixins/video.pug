mixin video(video)
    div 
        h4 
            a(href=`/videos/${video.id}`)=video.title
        p=video.description
        each hastag in video.hastags
            li=hashtag 
        small=video.createdAt
        hr